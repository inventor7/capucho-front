This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
components/ProductsBulkEdit.vue
components/ProductsDialog.vue
components/ProductsFilters.vue
components/ProductsHeader.vue
components/ProductsTable.vue
components/ProductsTable/ProductsTableEmpty.vue
index.ts
stores/fake/products.fakes.ts
types/products.types.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="components/ProductsBulkEdit.vue">
<template>
  <div
    v-if="selectedProducts.length > 0"
    class="bg-muted p-2 rounded-md mb-4 flex items-center justify-between"
  >
    <span class="text-sm">{{ selectedProducts.length }} products selected</span>
    <div class="flex gap-2">
      <Button variant="outline" size="sm">
        <ILucideEdit class="h-4 w-4 mr-2" />
        Bulk Edit
      </Button>
      <Button variant="destructive" size="sm" @click="handleBulkDelete">
        <ILucideTrash2 class="h-4 w-4 mr-2" />
        Delete Selected
      </Button>
    </div>
  </div>
</template>

<script setup lang="ts">
const selectedProducts = ref<number[]>([])

const handleBulkDelete = () => {
  console.log(`Deleting products: ${selectedProducts.value.join(', ')}`)
  selectedProducts.value = []
}
</script>
</file>

<file path="components/ProductsDialog.vue">
<template>
  <Dialog v-model:open="deleteDialogOpen">
    <DialogContent>
      <DialogHeader>
        <DialogTitle>Are you sure?</DialogTitle>
        <DialogDescription>
          This action cannot be undone. This will permanently delete the product from the database.
        </DialogDescription>
      </DialogHeader>
      <DialogFooter>
        <Button variant="outline" @click="deleteDialogOpen = false"> Cancel </Button>
        <Button variant="destructive" @click="handleDeleteConfirm"> Delete </Button>
      </DialogFooter>
    </DialogContent>
  </Dialog>
</template>

<script setup lang="ts">
const deleteDialogOpen = ref(false)
const productToDelete = ref<number | null>(null)

const handleDeleteConfirm = () => {
  console.log(`Deleting product ${productToDelete.value}`)
  deleteDialogOpen.value = false
  productToDelete.value = null
}
</script>
</file>

<file path="components/ProductsFilters.vue">
<template>
  <div class="flex flex-col md:flex-row gap-4 mb-6">
    <div class="flex-1 relative">
      <ILucideSearch
        class="absolute left-3 top-1/2 transform -translate-y-1/2 text-muted-foreground h-4 w-4"
      />
      <Input placeholder="Search products..." class="pl-10" v-model="searchTerm" />
    </div>
    <div class="flex gap-2">
      <div class="w-full">
        <Select v-model="selectedCategory">
          <SelectTrigger>
            <SelectValue placeholder="Category" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="all">All Categories</SelectItem>
            <SelectItem value="Living Room">Living Room</SelectItem>
            <SelectItem value="Dining">Dining</SelectItem>
            <SelectItem value="Bedroom">Bedroom</SelectItem>
            <SelectItem value="Kitchen">Kitchen</SelectItem>
            <SelectItem value="Office">Office</SelectItem>
            <SelectItem value="Outdoor">Outdoor</SelectItem>
          </SelectContent>
        </Select>
      </div>
      <div class="w-full">
        <Select v-model="selectedStatus">
          <SelectTrigger>
            <SelectValue placeholder="Status" />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="all">All Status</SelectItem>
            <SelectItem value="active">Active</SelectItem>
            <SelectItem value="out-of-stock">Out of Stock</SelectItem>
            <SelectItem value="discontinued">Discontinued</SelectItem>
          </SelectContent>
        </Select>
      </div>
      <Button variant="outline" size="icon">
        <ILucideFilter class="h-4 w-4" />
      </Button>
    </div>
  </div>
</template>

<script setup lang="ts">
const selectedStatus = ref('all')
const searchTerm = ref('')
const selectedCategory = ref('all')
</script>
</file>

<file path="components/ProductsHeader.vue">
<template>
  <div class="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
    <div>
      <h1 class="text-3xl font-heading font-bold">Products</h1>
      <p class="text-muted-foreground">Manage your products and inventory</p>
    </div>

    <div class="flex gap-2">
      <Button @click="$router.push('/products/new')">
        <ILucidePlus class="h-4 w-4 mr-2" />
        Add Product
      </Button>
      <Button variant="outline">
        <ILucideDownload class="h-4 w-4 mr-2" />
        Export
      </Button>
    </div>
  </div>
</template>

<script setup lang="ts"></script>
</file>

<file path="components/ProductsTable.vue">
<template>
  <Table>
    <TableHeader>
      <TableRow class="bg-card rounded-2xl">
        <TableHead class="w-12">
          <Checkbox
            :model-value="
              selectedProducts.length === sortedProducts.length && sortedProducts.length > 0
            "
            @update:model-value="handleSelectAll"
            aria-label="Select all products"
          />
        </TableHead>
        <TableHead class="w-20">Image</TableHead>
        <TableHead class="cursor-pointer" @click="handleSort('name')">
          <div class="flex items-center">
            Product
            <ILucideArrowUpDown
              v-if="sortField === 'name'"
              class="ml-2 h-4 w-4"
              :class="{ 'rotate-180': sortDirection === 'desc' }"
            />
          </div>
        </TableHead>
        <TableHead class="cursor-pointer" @click="handleSort('category')">
          <div class="flex items-center">
            Category
            <ILucideArrowUpDown
              v-if="sortField === 'category'"
              class="ml-2 h-4 w-4"
              :class="{ 'rotate-180': sortDirection === 'desc' }"
            />
          </div>
        </TableHead>
        <TableHead class="cursor-pointer" @click="handleSort('price')">
          <div class="flex items-center">
            Price
            <ILucideArrowUpDown
              v-if="sortField === 'price'"
              class="ml-2 h-4 w-4"
              :class="{ 'rotate-180': sortDirection === 'desc' }"
            />
          </div>
        </TableHead>
        <TableHead class="cursor-pointer" @click="handleSort('stock')">
          <div class="flex items-center">
            Stock
            <ILucideArrowUpDown
              v-if="sortField === 'stock'"
              class="ml-2 h-4 w-4"
              :class="{ 'rotate-180': sortDirection === 'desc' }"
            />
          </div>
        </TableHead>
        <TableHead>Status</TableHead>
        <TableHead class="text-right">Actions</TableHead>
      </TableRow>
    </TableHeader>
    <TableBody>
      <template v-if="sortedProducts.length > 0">
        <TableRow v-for="product in sortedProducts" :key="product.id">
          <TableCell>
            <Checkbox
              :model-value="selectedProducts.includes(product.id)"
              @update:model-value="
                (checked: boolean | 'intermediate') =>
                  handleSelectProduct(product.id, checked as boolean)
              "
              :aria-label="product.name"
            />
          </TableCell>
          <TableCell>
            <div class="relative w-12 h-12 rounded-md overflow-hidden">
              <img
                :src="product.image || '/placeholder.png'"
                :alt="product.name"
                class="object-cover w-full h-full"
              />
            </div>
          </TableCell>
          <TableCell>
            <div>
              <p class="font-medium">{{ product.name }}</p>
              <p class="text-sm text-muted-foreground">{{ product.woodType }} Wood</p>
              <Badge
                v-if="product.featured"
                variant="outline"
                class="mt-1 bg-secondary/10 text-secondary border-secondary/20"
              >
                Featured
              </Badge>
            </div>
          </TableCell>
          <TableCell>{{ product.category }}</TableCell>
          <TableCell>
            <template v-if="product.salePrice">
              <div>
                <span class="font-medium">${{ product.salePrice }}</span>
                <span class="text-sm text-muted-foreground line-through ml-2"
                  >${{ product.price }}</span
                >
              </div>
            </template>
            <template v-else>
              <span class="font-medium">${{ product.price }}</span>
            </template>
          </TableCell>
          <TableCell>
            <div class="flex items-center">
              <span
                :class="`w-2 h-2 rounded-full mr-2 ${
                  product.stock > 5
                    ? 'bg-green-500'
                    : product.stock > 0
                      ? 'bg-yellow-500'
                      : 'bg-red-500'
                }`"
              ></span>
              <span>{{ product.stock }}</span>
            </div>
          </TableCell>
          <TableCell>
            <Badge
              :class="{
                'bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300':
                  product.status === 'active',
                'bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300':
                  product.status === 'out-of-stock',
                'bg-gray-100 text-gray-800 dark:bg-gray-800 dark:text-gray-300':
                  product.status === 'discontinued',
              }"
            >
              {{
                product.status === 'active'
                  ? 'Active'
                  : product.status === 'out-of-stock'
                    ? 'Out of Stock'
                    : 'Discontinued'
              }}
            </Badge>
          </TableCell>
          <TableCell class="text-right">
            <DropdownMenu>
              <DropdownMenuTrigger as-child>
                <Button variant="ghost" size="icon">
                  <ILucideMoreHorizontal class="h-4 w-4" />
                  <span class="sr-only">Open menu</span>
                </Button>
              </DropdownMenuTrigger>
              <DropdownMenuContent align="end">
                <DropdownMenuLabel>Actions</DropdownMenuLabel>
                <DropdownMenuItem @click="router.push(`/products/${product.id}`)">
                  <ILucideEye class="h-4 w-4 mr-2" />
                  View
                </DropdownMenuItem>
                <DropdownMenuItem @click="router.push(`/products/${product.id}/edit`)">
                  <ILucideEdit class="h-4 w-4 mr-2" />
                  Edit
                </DropdownMenuItem>
                <DropdownMenuSeparator />
                <DropdownMenuItem class="text-red-600" @click="handleDeleteClick(product.id)">
                  <ILucideTrash2 class="h-4 w-4 mr-2" />
                  Delete
                </DropdownMenuItem>
              </DropdownMenuContent>
            </DropdownMenu>
          </TableCell>
        </TableRow>
      </template>
      <ProductsTableEmpty v-else />
    </TableBody>
  </Table>
</template>

<script setup lang="ts">
const router = useRouter()

const selectedProducts = ref<number[]>([])

const selectedStatus = ref('all')
const searchTerm = ref('')
const sortField = ref('name')
const sortDirection = ref<'asc' | 'desc'>('asc')
const selectedCategory = ref('all')

const deleteDialogOpen = ref(false)
const productToDelete = ref<number | null>(null)

const filteredProducts = computed(() => {
  return products.filter((product) => {
    const matchesSearch =
      product.name.toLowerCase().includes(searchTerm.value.toLowerCase()) ||
      product.woodType.toLowerCase().includes(searchTerm.value.toLowerCase())
    const matchesCategory =
      selectedCategory.value === 'all' || product.category === selectedCategory.value
    const matchesStatus = selectedStatus.value === 'all' || product.status === selectedStatus.value

    return matchesSearch && matchesCategory && matchesStatus
  })
})

const sortedProducts = computed(() => {
  return [...filteredProducts.value].sort((a, b) => {
    let aValue = a[sortField.value as keyof typeof a]
    let bValue = b[sortField.value as keyof typeof b]

    if (!aValue || !bValue) return 0

    if (sortField.value === 'price') {
      aValue = a.salePrice || a.price
      bValue = b.salePrice || b.price
    }

    if (aValue < bValue) return sortDirection.value === 'asc' ? -1 : 1
    return 0
  })
})

const handleSort = (field: string) => {
  if (field === sortField.value) {
    sortDirection.value = sortDirection.value === 'asc' ? 'desc' : 'asc'
  } else {
    sortField.value = field
    sortDirection.value = 'asc'
  }
}

const handleSelectAll = (checked: boolean) => {
  if (checked) {
    selectedProducts.value = sortedProducts.value.map((product) => product.id)
  } else {
    selectedProducts.value = []
  }
}

const handleSelectProduct = (productId: number, checked: boolean) => {
  if (checked) {
    selectedProducts.value = [...selectedProducts.value, productId]
  } else {
    selectedProducts.value = selectedProducts.value.filter((id) => id !== productId)
  }
}

const handleDeleteClick = (productId: number) => {
  productToDelete.value = productId
  deleteDialogOpen.value = true
}
</script>
</file>

<file path="components/ProductsTable/ProductsTableEmpty.vue">
<template>
  <TableRow class="hover:bg-background">
    <TableCell :colspan="12" class="h-24 text-center p-0">
      <Empty class="from-muted/50 to-background h-full bg-linear-to-b from-50% rounded-none">
        <EmptyHeader>
          <EmptyMedia variant="icon">
            <ILucideBox />
          </EmptyMedia>
          <EmptyTitle>No products found</EmptyTitle>
          <EmptyDescription> Try adjusting your search or filters. </EmptyDescription>
        </EmptyHeader>
        <EmptyContent>
          <Button variant="outline" size="sm">
            <ILucideRefreshCcw />
            Refresh
          </Button>
        </EmptyContent>
      </Empty>
    </TableCell>
  </TableRow>
</template>

<script setup lang="ts"></script>
</file>

<file path="index.ts">
export type { Product } from './types/products.types'
</file>

<file path="stores/fake/products.fakes.ts">
export const products: Product[] = [
  {
    id: 1,
    name: 'Walnut Dining Table',
    slug: 'walnut-dining-table',
    category: 'Dining',
    price: 1299,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Walnut',
    stock: 5,
    status: 'active',
    featured: true,
  },
  {
    id: 2,
    name: 'Oak Bookshelf',
    slug: 'oak-bookshelf',
    category: 'Living Room',
    price: 899,
    salePrice: 799,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Oak',
    stock: 8,
    status: 'active',
    featured: true,
  },
  {
    id: 3,
    name: 'Mahogany Coffee Table',
    slug: 'mahogany-coffee-table',
    category: 'Living Room',
    price: 649,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Mahogany',
    stock: 12,
    status: 'active',
    featured: false,
  },
  {
    id: 4,
    name: 'Cherry Wood Bed Frame',
    slug: 'cherry-wood-bed-frame',
    category: 'Bedroom',
    price: 1499,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Cherry',
    stock: 3,
    status: 'active',
    featured: true,
  },
  {
    id: 5,
    name: 'Maple Kitchen Cabinet',
    slug: 'maple-kitchen-cabinet',
    category: 'Kitchen',
    price: 2199,
    salePrice: 1899,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Maple',
    stock: 2,
    status: 'active',
    featured: true,
  },
  {
    id: 6,
    name: 'Teak Outdoor Table',
    slug: 'teak-outdoor-table',
    category: 'Outdoor',
    price: 899,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Teak',
    stock: 7,
    status: 'active',
    featured: false,
  },
  {
    id: 7,
    name: 'Pine Wardrobe',
    slug: 'pine-wardrobe',
    category: 'Bedroom',
    price: 1099,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Pine',
    stock: 4,
    status: 'active',
    featured: false,
  },
  {
    id: 8,
    name: 'Ebony Desk',
    slug: 'ebony-desk',
    category: 'Office',
    price: 1299,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Ebony',
    stock: 0,
    status: 'out-of-stock',
    featured: false,
  },
  {
    id: 9,
    name: 'Rosewood Dining Chairs (Set of 4)',
    slug: 'rosewood-dining-chairs',
    category: 'Dining',
    price: 1599,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Rosewood',
    stock: 2,
    status: 'active',
    featured: false,
  },
  {
    id: 10,
    name: 'Walnut TV Stand',
    slug: 'walnut-tv-stand',
    category: 'Living Room',
    price: 849,
    image: '/placeholder.png?height=600&width=800',
    woodType: 'Walnut',
    stock: 6,
    status: 'active',
    featured: false,
  },
]
</file>

<file path="types/products.types.ts">
export interface Product {
  id: number
  name: string
  slug: string
  category: string
  price: number
  salePrice?: number
  image: string
  woodType: string
  stock: number
  status: 'active' | 'out-of-stock' | 'discontinued'
  featured: boolean
}
</file>

</files>
